# Agent notes

This repo is an OBS AirPlay **receiver** (helper + thin plugin).

Read `docs/KNOWLEDGE.md` before changing decode, UxPlay, IPC, CMake, or install. Project rule: `.cursor/rules/obs-airplay.mdc`.
